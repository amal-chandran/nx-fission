{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Publish a fission service",
  "description": "",
  "type": "object",
  "properties": {
    "stage": {
      "type": "string",
      "description": "stage where deployment happens",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "default": "development",
      "x-prompt": "What stage would you like to deploy?"
    }
  },
  "required": []
}
